apiVersion: v1
kind: Service
metadata:
  name: {{ include "chart-samples.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "chart-samples.name" . }}
    helm.sh/chart: {{ include "chart-samples.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "chart-samples.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
